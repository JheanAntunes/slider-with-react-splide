export const mainOptions = {
  type: 'loop',
  perPage: 2,
  perMove: 1,
  pagination: false,
  gap: '2rem'
}

export const thumbsOptions = {
  type: 'slide',
  rewind: true,
  gap: '2rem',
  pagination: false,
  fixedWidth: 120,
  fixedHeight: 60,
  isNavigation: true
}
